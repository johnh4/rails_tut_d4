<%= provide(:title, "All Users") %>

<div class="row">
	<h1>All Users</h1>
	<% if @users.any? %>
		<%= will_paginate %>
		<ul class="users">
			<%= render @users %>
		</ul>
		<%= will_paginate %>
	<% end %>
</div>